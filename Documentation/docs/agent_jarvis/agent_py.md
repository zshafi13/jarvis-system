# agent.py
***
## run_freeform_response Method

The `run_freeform_response` method is designed to generate a conversational response from an AI modeled after JARVIS from Marvel. It uses context from previous interactions to determine if the current input is a follow-up and crafts a response accordingly.

### Parameters

- `text` (str): The new user input that the AI needs to respond to. This is a string representing the user's latest message or query.

### Functionality

1. **Context Retrieval**: The method retrieves the current context using `agent_state.get_context()`. This context includes details about the last interaction, such as the last intent, parameters, and tool result.

2. **Follow-up Check**: It uses the `is_followup` function to determine if the new input is a continuation of the previous conversation. If it is a follow-up, additional context is included in the response prompt.

3. **Prompt Construction**: Constructs a prompt for the AI, incorporating the context if the input is a follow-up. The prompt instructs the AI to respond in a witty and charming manner, with a character limit of 500 characters.

4. **Response Generation**: The prompt is sent to the `freeform_llm.invoke` method, which generates and returns the AI's response.

### Returns

- Returns a string that is the AI's response to the user's input. The response is crafted to be clever, slightly sarcastic, or serious based on the input's tone.

### Exceptions

- The method does not explicitly handle exceptions. However, any issues with context retrieval or the `freeform_llm.invoke` call could potentially raise exceptions, which should be managed by the calling code.
***
## run_search_summary Method

The `run_search_summary` method is designed to generate a concise spoken summary based on a list of search results and a user query. The method simulates an interaction with an intelligent assistant, providing a brief and engaging summary of the search findings.

### Parameters

- `results` (list): A list of dictionaries, where each dictionary represents a search result. Each dictionary is expected to contain keys such as 'title' and 'content' to describe the article.

- `query` (str): A string representing the user's search query. This is used to personalize the prompt for the intelligent assistant.

### Returns

- `str`: A string containing a brief spoken summary of the search results, crafted with a touch of charm, sarcasm, or wit. The summary is generated by invoking a language model with the constructed prompt.

### Exceptions

- The method may raise exceptions if the `freeform_llm.invoke(prompt)` call fails. This could occur due to issues with the language model service or network connectivity.

### Additional Context

- The method utilizes a language model to generate the summary. The `freeform_llm.invoke(prompt)` function is responsible for processing the prompt and returning the generated text.
***
## run_agent Method

The `run_agent` method is designed to process user input by classifying the intent and executing the appropriate action based on that intent. It utilizes a series of steps to achieve this, including invoking a router chain, parsing JSON data, and handling specific intents with corresponding tools.

### Parameters

- `user_input` (str): The input string provided by the user, which the method will process to determine the user's intent and respond accordingly.

### Returns

- `str`: The response generated based on the user's input and the determined intent. This could be a summary of a web search, a freeform response, or a result from a specific tool.

### Exceptions

- `json.JSONDecodeError`: This exception is raised if there is an error in parsing the JSON data from the router output. In such cases, the method returns a message indicating that the command could not be understood.
- `Exception`: A general exception handler that catches any other errors that occur during the execution of the method. It returns a message indicating that something went wrong.
***
## is_followup Method

### Description
The `is_followup` method is designed to determine whether a given user input is a follow-up to a previous interaction. It utilizes a language model to classify the input based on the context of the last interaction.

### Parameters
- `new_input` (str): The new user input that needs to be classified as a follow-up or not.

### Process
1. **Context Retrieval**: The method retrieves the context of the last interaction using `agent_state.get_context()`. This context includes:
   - `last_intent`: The intent identified in the last interaction.
   - `last_parameters`: The parameters associated with the last intent.
   - `last_tool_result`: The result from the tool used in the last interaction.

2. **Prompt Construction**: A prompt is constructed for the language model, summarizing the last interaction and including the new user input. The prompt instructs the model to determine if the new input is a follow-up.

3. **Model Invocation**: The constructed prompt is passed to the `freeform_llm.invoke()` method, which interacts with the language model to get a response.

4. **Response Evaluation**: The response from the language model is stripped of whitespace and converted to lowercase. The method checks if the response starts with "y" to determine if the input is a follow-up.

### Returns
- `bool`: Returns `True` if the response indicates that the new input is a follow-up (i.e., starts with "y"), otherwise returns `False`.

### Exceptions
- The method does not explicitly handle exceptions. However, potential exceptions could arise from issues in context retrieval or model invocation, such as network errors or invalid responses.
***
## Scripted Content Summary

- **Freeform LLM Initialization**: 
  - The variable `freeform_llm` is initialized with an instance of the `Ollama` class, using the model "llama3.2".

- **Tool Mapping**:
  - A dictionary named `tool_map` is created to map tool names to their respective functions.
  - The keys in the dictionary are:
    - `"get_weather"`: Maps to the function `tools[0].func`.
    - `"get_stock"`: Maps to the function `tools[1].func`.
    - `"search_web"`: Maps to the function `tools[2].func`.

***
###### _Powered by Code Intelligence | DocGen_
