# agent.py
***
## run_freeform_response Method

### Description
The `run_freeform_response` method generates a conversational response based on user input, leveraging context from previous interactions if applicable. It simulates the personality of Jarvis, the AI from Marvel, providing responses that are witty, charming, and slightly sarcastic unless a serious tone is required.

### Parameters
- `text` (str): The new user input that the method will process to generate a response. This input is analyzed to determine if it is a follow-up to a previous conversation.

### Process
1. **Context Retrieval**: The method retrieves the current context using `agent_state.get_context()`, which includes details of the last interaction such as the intent, parameters, and tool results.
2. **Follow-up Check**: It uses the `is_followup` method to determine if the current input is a continuation of the previous conversation.
3. **Contextual Prompt Construction**: If the input is a follow-up and relevant context exists, a contextual prompt is constructed to inform the response generation process.
4. **Response Generation**: A prompt is created for the AI, detailing the user input and any relevant context. The AI is instructed to respond in a manner consistent with its personality, considering the nature of the input (e.g., jokes for humor requests, greetings for salutations).

### Output
- Returns a `str`: The generated response from the AI, which is processed through `freeform_llm.invoke(prompt).strip()`. The response is concise, under 500 characters, and tailored to the nature of the user input.

### Exceptions
- The method does not explicitly handle exceptions within its implementation. Any exceptions would likely arise from the `freeform_llm.invoke` method or context retrieval, which are assumed to be managed elsewhere in the system.
***
## run_search_summary Method

### Description
The `run_search_summary` method generates a brief spoken response summarizing key takeaways from a list of articles based on a user's query. It is designed to mimic the style of the JARVIS AI from the Marvel Cinematic Universe, incorporating charm, sarcasm, or wit.

### Parameters
- **results**: `list`
  - A list of dictionaries, each representing an article. The dictionary should contain keys such as 'title' and 'content'.
  
- **query**: `str`
  - A string representing the user's search query.

### Returns
- **str**: 
  - A string containing a brief spoken response summarizing the articles' key takeaways, formatted with a touch of charm, sarcasm, or wit.

### Exceptions
- The method assumes that the `freeform_llm.invoke` function is available and correctly configured to process the prompt. If this function is not available or fails, an exception may be raised.
***
## run_agent Method

The `run_agent` method is designed to process user input, classify the intent, and execute the appropriate action based on the identified intent. It leverages various tools and methods to achieve this, including invoking external services and handling different types of intents.

### Parameters

- `user_input` (str): The input string provided by the user. This is the primary data that the method processes to determine the user's intent and respond accordingly.

### Process Overview

1. **Intent Classification**: 
   - The method uses `router_chain.invoke` to classify the intent of the `user_input`. This involves passing the input along with the current context obtained from `agent_state.get_context()`.

2. **Result Parsing**:
   - The raw output from the intent classification is cleaned and parsed. This involves removing unnecessary formatting and extracting JSON data to identify the intent and any associated parameters.

3. **Intent Handling**:
   - Depending on the identified intent, the method performs different actions:
     - **Freeform Intent**: Calls `run_freeform_response` with the query parameter.
     - **Search Web Intent**: Executes a web search using the `search_web` tool and summarizes the results using `run_search_summary`.
     - **Other Intents**: Uses the appropriate tool from `tool_map` to handle other recognized intents.

4. **Error Handling**:
   - The method includes error handling for JSON parsing errors and general exceptions, providing user-friendly error messages when issues occur.

### Returns

- The method returns a string response based on the processed intent. This could be a summary of search results, a response generated by a tool, or an error message if something goes wrong.

### Exceptions

- `json.JSONDecodeError`: Raised if there is an error in parsing the JSON data from the router output.
- `Exception`: A general exception handler captures any other errors that may occur during the execution of the method, ensuring that the user receives a coherent error message.
***
## is_followup Method

The `is_followup` method is designed to determine if a given user input is a follow-up to a previous interaction. It utilizes a language model to classify the input based on the context of the last interaction.

### Parameters

- `new_input` (str): This parameter represents the new user input that needs to be classified as a follow-up or not. It is a string containing the user's latest message.

### Returns

- `bool`: The method returns a boolean value. It returns `True` if the new input is classified as a follow-up to the previous interaction, and `False` otherwise.

### Exceptions

- The method does not explicitly handle exceptions, but it relies on the `freeform_llm.invoke` method, which may raise exceptions if the language model invocation fails or if there are issues with the input prompt.

### Additional Context

- The method retrieves the context of the last interaction using `agent_state.get_context()`, which includes details such as the last intent, parameters, and tool result.
- The `freeform_llm.invoke` function is used to interact with a language model that processes the prompt and determines if the new input is a follow-up.
***
## Scripted Content Summary

- The program initializes a language model named `freeform_llm` using the `Ollama` class with the model parameter set to `"llama3.2"`.
- A dictionary named `tool_map` is created to map string keys to specific functions from a `tools` list. 
  - `"get_weather"` is mapped to the first function in the `tools` list.
  - `"get_stock"` is mapped to the second function in the `tools` list.
  - `"search_web"` is mapped to the third function in the `tools` list.

***
###### _Powered by Code Intelligence | DocGen_
